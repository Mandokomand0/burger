<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Devour IT!{{/if}}
	</button>

	<button class="eat-burger" data-id="{{this.id}}">DEVOURE!</button>
</li>
